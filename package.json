{
  "name": "total-viewer",
  "version": "1.0.0",
  "description": "Total Viewer — Professional DXF Visualization & Analysis Tool",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"PYTHON,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev:python\" \"npm run dev:frontend\"",
    "dev:python": "cd python-backend && uvicorn main:app --host 127.0.0.1 --port 8000 --reload",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "dxf-parser": "^1.1.2",
    "earcut": "^3.0.2",
    "three": "^0.158.0",
    "three-dxf": "^1.3.1",
    "three-stdlib": "^2.36.1"
  }
}
